# Authentication Microservice (Auth-MS)

This directory will contain the service responsible for user authentication,
including registration, login, token generation, and validation.
added to check git 

plus also we want to protect our logs from unauthorized access and only authorized users should be able to access the logs

so we will use JWT tokens to authenticate users and only users with valid tokens will be able to access the logs

and reduce possibilities of IDOR attacks mostly as we saw that our endpoints could be targets mostly